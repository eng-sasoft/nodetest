<html>
<head>
    <title>
        <%if(allAddresses){%>
        <%=allAddresses.title%>
         <%}else{%>
            no Title
        <%}%>
    </title>
    </head>
<body>

    
<%if(allAddresses){%>
    <h1> Following are the titles of given websites: </h1>
    <%if(allAddresses instanceof Array){%>
    <ul>
        <%for(var i=0; i<allAddresses.length; i++){%>
            <li><%=allAddresses[i].url%> - <%if(allAddresses[i].event){%>Event-<%=allAddresses[i].domain%><%}else{%> <%=allAddresses[i].domain%><%}%></li>
        <%}%>
    </ul>
    
    <%}else {%>
        <p> <%=allAddresses.url%> - <%if(allAddresses.event){%>Event-<%=allAddresses.domain%><%}else{%> <%=allAddresses.domain%><%}%></p>
    <%}%>
<%}else{%>
        <p>Enter query String like ?address=www.google.com or address=google.com etc</p>
    <%}%>
</body>
</html>